#! /bin/bash 

# file -x => nos muestra linea por linea su ejecucion
# tambien podemos agregar -x despues de /bind/bash -x

# podemos usar set para establecer un delimitador

set -x
    echo "testing"
set +x